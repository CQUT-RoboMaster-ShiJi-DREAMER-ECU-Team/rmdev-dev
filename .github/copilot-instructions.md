# Copilot instructions

本仓库将智能体规则与技能文档统一放在 `.agents/` 目录下维护。

## 路径重定向说明

当需要读取以下内容时，请优先从 `.agents/` 路径读取：

- skills: `.agents/skills/**/SKILL.md`
- instructions: `.agents/instructions/**`

若仓库中同时存在其他历史路径副本，请以 `.agents/` 下内容为准。
