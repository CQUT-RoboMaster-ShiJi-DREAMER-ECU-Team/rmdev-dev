# device_model

`rmdev` 设备抽象模型模块，提供电机、传感器等设备的数据模型与统一接口。

## 依赖

- `emdevif_core`

## 功能

- 设备数据模型抽象（例如电机、IMU）
- 供驱动层与算法层共享统一数据结构

## 使用建议

- 驱动侧负责填充模型数据
- 算法侧消费模型数据，避免直接耦合底层外设句柄

## 说明

该模块无独立配置项，通常与驱动模块、`ins` 模块联合使用。
